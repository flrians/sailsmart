---
description: Stage, commit, and push all changes to GitHub (origin main)
---

Push the current code to GitHub. Use the Bash tool to:
1. Run `git status` to show what has changed
2. Stage all changes with `git add -A`
3. Create a commit with a short descriptive message summarising what changed
4. Push to the remote with `git push origin main`

Report what was committed and confirm the push succeeded.

---
description: Start the Next.js development server on localhost:3000
---

Start the Next.js development server by running the `./start_server` script in the project root. Use the Bash tool to execute it, then verify the server started by checking for a running Next.js process.

---
description: Pull the latest code from GitHub (origin main)
---

Pull the latest code from GitHub. Use the Bash tool to run `git pull origin main` and report what changed (new files, modified files, or confirmation that everything is already up to date).
